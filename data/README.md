# Data

The prototype downloads adjusted daily market prices programmatically. Locally cached data belongs in `data/cache/` and is not committed by default.

The README will document the exact provider, tickers, fields, download date, sample period, missing-value treatment and known limitations once the data pipeline is implemented.

